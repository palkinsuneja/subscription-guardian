# Core detection
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Backend + dashboard
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
streamlit>=1.32

# AI agent (optional at runtime — falls back to templates if key absent)
groq>=0.11

# Razorpay test-mode integration (optional)
razorpay>=1.4
